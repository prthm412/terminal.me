export default function Hero() {
  return (
    <section id="hero" className="min-h-screen flex items-center justify-center px-6">
      <span className="font-mono text-muted text-sm">// hero — phase 2</span>
    </section>
  );
}
