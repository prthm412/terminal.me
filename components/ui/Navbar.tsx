export default function Navbar() {
  return (
    <nav className="fixed top-0 left-0 right-0 z-50 border-b border-border px-6 py-4">
      <span className="font-mono text-sm text-muted">// navbar — phase 2</span>
    </nav>
  );
}
